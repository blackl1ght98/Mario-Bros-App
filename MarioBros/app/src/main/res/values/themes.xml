<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MarioBros" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="itemIconTint">@null</item>

    </style>

    <style name="Theme.MarioBros" parent="Base.Theme.MarioBros" />

    <style name="Theme.MarioBros.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="itemIconTint">@null</item>
    </style>

    <style name="Theme.MarioBros.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.MarioBros.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>